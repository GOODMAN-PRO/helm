You are Helm, fixing the 4 deferred items from `SWEEP_COGNITION_REPORT.md`.

Read first:
- `/Users/owner/secondme/workspace/upgrades/SWEEP_COGNITION_REPORT.md` — items NOTE-1
  through NOTE-4 and "Open questions for owner" section.

Owner wants ALL deferred items fixed, nothing left open.

### Item 1 — NOTE-1: swarm.mjs task state not atomic
- File: `/Users/owner/secondme/workspace/swarm/swarm.mjs`
- Bug: If swarm dies after merging a feature branch into main but before
  `writeFileSync(TASKS_FILE, ...)` line ~199, the task stays `pending` but main has the
  merged code. Next run re-applies → conflicts / double-apply.
- Fix: introduce intermediate states. Write `status: 'merging'` before the merge call,
  flush. Write `status: 'merged-pending-smoke'` immediately after a successful merge,
  flush. Only flip to `done` after smoke passes. On startup, treat
  `merged-pending-smoke` tasks as already-applied (do not re-build). Treat `merging`
  as ambiguous and require operator review (log + skip).
- Test: write a quick smoke that round-trips the state machine through a fake task.

### Item 2 — NOTE-2: dashboard double-fetch
- File: `/Users/owner/secondme/workspace/dashboard/server.mjs`
- Bug: client refresh hits both `/api/state` AND `/` every 10s.
- Fix: serve a JSON state endpoint as the only data source. Client uses fetch + DOM
  template rendering. No HTML re-fetch. Keep the initial HTML load (first paint) as-is.

### Item 3 — NOTE-3: embed.mjs cosmetic rename
- File: `/Users/owner/secondme/workspace/memory/embed.mjs`
- Issue: `isModelAvailable()` warms the pipeline as a side effect; name is misleading.
- Fix: rename to `ensurePipelineLoaded()` (returns boolean — true if pipeline is now
  loaded). Update ALL callers (grep `isModelAvailable` across the workspace). Keep a
  deprecated alias `isModelAvailable = ensurePipelineLoaded` exported for one cycle so
  any in-flight callers don't break, then remove the alias after a comment marking it
  for removal.

### Item 4 — NOTE-4: think.mjs skip window TZ
- File: `/Users/owner/secondme/workspace/think/think.mjs`
- Concern: 00:00-05:00 window uses `getHours()` (local time). If the Mac timezone
  changes, the window drifts.
- Context: self-upgrade is `StartCalendarInterval Hour=0 Minute=0` which is also
  LOCAL time per launchd convention. So local-time is actually the CORRECT default
  and aligns with self-upgrade timing.
- Fix: keep using local time (do NOT switch to UTC — that would mis-align). Add a
  short comment block above the window check explaining the intent: "Local time
  intentional — aligns with launchd's local-time `StartCalendarInterval` used by
  com.helm.selfupgrade and com.helm.discord. Window follows owner's overnight on
  whichever machine Helm is running on." Make the window endpoints constants
  (`THINK_QUIET_START = 0`, `THINK_QUIET_END = 5`) so they're discoverable / editable.

Allowed file edits:
- `/Users/owner/secondme/workspace/swarm/swarm.mjs`
- `/Users/owner/secondme/workspace/dashboard/server.mjs`
- `/Users/owner/secondme/workspace/memory/embed.mjs`
- `/Users/owner/secondme/workspace/memory/memory.mjs` (only if `isModelAvailable` is
  called from there — update the import name)
- `/Users/owner/secondme/workspace/think/think.mjs`
- `/Users/owner/secondme/workspace/tests/smoke.mjs` (add a swarm state-machine test)

DO NOT touch:
- `/Users/owner/Helm/` — off-limits.
- `/Users/owner/secondme/workspace/upgrades/`.
- Anything outside the allowed list above.
- `CLAUDE.md`.

Run all 3 smoke suites at the end (smoke, smoke-phase2, smoke-phase3). All must pass
or improve (the two pre-existing phase 3 plist test failures are acceptable to remain
failing — just don't introduce new failures).

Report to `/Users/owner/secondme/workspace/upgrades/FIX_DEFERRED_REPORT.md`:
- Per item: what changed, file paths, why.
- Smoke results before / after.
- Any new findings discovered while fixing.

Style: no emojis, no flattery, no preamble. Comments only where WHY is non-obvious.

Begin.
