[
  { "id": "sweep-index",        "title": "bug-sweep index.js",                         "status": "pending", "spec": "TARGET: index.js (Discord adapter). Find REAL bugs only: races, unhandled rejections, leaked timers/child processes, bad error handling, path/quoting/injection, secret leakage, broken edge cases. Fix MINIMALLY (no refactors, no renames, no features). If you fix a real bug, add a unique-numbered check in workspace/tests/smoke.mjs that would have caught it. Keep `node workspace/tests/smoke.mjs` green. If NO real bug, change NOTHING and write 'no bug found'. NEVER touch ~/helm. Output: each bug + its one-line fix." },
  { "id": "sweep-imessage",     "title": "bug-sweep imessage.js",                      "status": "pending", "spec": "TARGET: imessage.js (iMessage adapter + attributedBody decoder + chat.db polling). Same rules as the index sweep: real bugs only, minimal surgical fixes, add a smoke check if you fix one, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-sessions",     "title": "bug-sweep sessions.mjs",                     "status": "pending", "spec": "TARGET: workspace/sessions.mjs (sqlite session store). Real bugs only (sql, concurrency, null handling). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-memory",       "title": "bug-sweep memory.mjs",                       "status": "pending", "spec": "TARGET: workspace/memory/memory.mjs (facts/episodes/recall, keyword+TF-IDF+embedding blend). Real bugs only (recall ranking, sql, the async wrapper). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-embed",        "title": "bug-sweep embed.mjs",                        "status": "pending", "spec": "TARGET: workspace/memory/embed.mjs (local embeddings). Real bugs only (lazy load guard, cosine math, vector cache, model-absent fallback). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-consolidate",  "title": "bug-sweep consolidate.mjs",                  "status": "pending", "spec": "TARGET: workspace/memory/consolidate.mjs (decay/dedupe/distil). Real bugs only (decay logic, protected-source handling, dedupe merge). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-refresh",      "title": "bug-sweep refresh-index.mjs",                "status": "pending", "spec": "TARGET: workspace/memory/refresh-index.mjs (writes INDEX.md). Real bugs only (grouping, null fields, file write). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-cron",         "title": "bug-sweep scheduler/cron.mjs",               "status": "pending", "spec": "TARGET: workspace/scheduler/cron.mjs (cron match + nextCronDate, UTC). Real bugs only (DST/UTC, field ranges, impossible-cron handling, off-by-one). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-scheduler",    "title": "bug-sweep scheduler runner",                 "status": "pending", "spec": "TARGET: the scheduler runner/daemon in workspace/scheduler/ (the .mjs that loads jobs and fires them; NOT cron.mjs). Real bugs only (job loop, overlap, timezone, jobs.db). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-runs",         "title": "bug-sweep runs/runs.mjs",                    "status": "pending", "spec": "TARGET: workspace/runs/runs.mjs (run-dir bookkeeping). Real bugs only (path building, race on mkdir, cleanup). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-tools",        "title": "bug-sweep tools/tools.mjs",                  "status": "pending", "spec": "TARGET: workspace/tools/tools.mjs (registry load + dispatch + confirm gating). Real bugs only (arg parsing, --force confirm bypass, registry validation, error propagation). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-browser",      "title": "bug-sweep browser tools",                    "status": "pending", "spec": "TARGET: workspace/tools/impl/browser-shared.mjs and browser.*.mjs. Real bugs only (lazy playwright import, context/profile leaks, state file races, isMain guard). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-vision",       "title": "bug-sweep vision.mjs",                       "status": "pending", "spec": "TARGET: workspace/tools/impl/vision.mjs (screencap + claude image read + Retina scale). Real bugs only (scale math, temp-file cleanup, missing-permission handling). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-plans",        "title": "bug-sweep plans/plan.mjs",                   "status": "pending", "spec": "TARGET: workspace/plans/plan.mjs (sqlite planning). Real bugs only (step ordering/idx, status transitions, next-step query, sql). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-think",        "title": "bug-sweep think.mjs",                        "status": "pending", "spec": "TARGET: workspace/think/think.mjs (24/7 cognition, weekly review, locks). Real bugs only (stale-lock detection, quiet-window math, weekly marker, overlap). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-selfupgrade",  "title": "bug-sweep self-upgrade.mjs",                 "status": "pending", "spec": "TARGET: workspace/upgrades/self-upgrade.mjs (git snapshot, claude self-improve, smoke gate, rollback). Real bugs only (rollback correctness, lock handling, health-check, timeout). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-swarm",        "title": "bug-sweep swarm.mjs",                        "status": "pending", "spec": "TARGET: workspace/swarm/swarm.mjs (this engine). Real bugs only (pool concurrency, worktree/branch cleanup leaks, merge/abort correctness, resolver path, verdict parsing). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-dashboard",    "title": "bug-sweep dashboard/server.mjs",             "status": "pending", "spec": "TARGET: workspace/dashboard/server.mjs (node:http dashboard). Real bugs only (request handling, sql reads, launchctl parsing, unescaped HTML/XSS from data, port binding, error handling). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-bin",          "title": "bug-sweep bin/ scripts",                     "status": "pending", "spec": "TARGET: bin/helm-push.mjs and any other scripts in bin/ (NOT the compiled guicontrol binary). Real bugs only (arg handling, env/secret handling, error propagation, exit codes). Minimal fixes, smoke check if fixed, keep smoke green, no bug => no change, never touch ~/helm. Output each bug + fix." },
  { "id": "sweep-mcp-tests",    "title": "bug-sweep mcp wiring + smoke coverage",      "status": "pending", "spec": "TARGET: workspace/mcp/servers.json + the mcpConfigArg() usage in index.js/imessage.js, AND audit workspace/tests/smoke.mjs for coverage GAPS (behaviors with no check). Real bugs only (bad server config that would hang, missing fallback). Add missing smoke checks for clearly-untested critical behavior. Minimal changes, keep smoke green, never touch ~/helm. Output: config bugs found + coverage gaps + checks added." }
]
