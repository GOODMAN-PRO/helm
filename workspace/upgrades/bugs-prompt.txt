You are Helm, fixing the Phase 1 review bugs autonomously.

Read first:
- `/Users/owner/secondme/workspace/upgrades/PHASE1_REVIEW.md` — the audit. Read it fully.
- `/Users/owner/secondme/workspace/upgrades/PLAN.md` — the spec.
- `/Users/owner/secondme/workspace/upgrades/PHASE2_REPORT.md` — note: this report says the
  dispatcher confirm gate (BUG-3) was already implemented in phase 2. Verify by reading
  `/Users/owner/secondme/workspace/tools/tools.mjs` — if the gate is real (exits 2 on
  confirm:true without --force) then BUG-3 is fixed; just add a smoke test for it.

Fix these bugs:

### BUG-1 (HIGH) — Cron timezone
`workspace/scheduler/cron.mjs` uses local-time methods (getHours, getMinutes, etc).
PLAN.md and CLAUDE.md document cron as UTC. Switch the parser to UTC methods
(`getUTCHours`, `getUTCMinutes`, `getUTCDate`, `getUTCMonth`, `getUTCDay`) so it matches
the documented behaviour. Also: there is a demo job `good-morning` in jobs.db with
cron `0 2 * * 1-5` (intended 09:00 GMT+7 = 02:00 UTC). Confirm this is now correct after
the fix.

### BUG-2 (MED) — migrate.mjs duplicates on re-run
`workspace/memory/migrate.mjs` uses `ON CONFLICT DO NOTHING` against a table with no
unique constraint on (kind, key). Re-running duplicates facts. Fix one of:
(a) add a unique index on (kind, key) to the facts table and keep ON CONFLICT, OR
(b) replicate the check-then-insert-or-update pattern from memory.mjs's `remember`.
Either is fine. After the fix, run migrate twice and confirm fact count doesn't grow.
If existing duplicates are in memory.db right now, dedupe them once (keep lowest id).

### BUG-3 (MED) — confirm gate
Probably already fixed by phase 2. Verify by inspecting tools.mjs and add a smoke test
that calls a confirm:true tool WITHOUT --force and asserts a non-zero exit + a
"CONFIRM" string in stderr. If the gate is missing, implement it per PLAN.md §1.3.

### BUG-4 (LOW) — heartbeat timer leak in index.js
`/Users/owner/secondme/index.js` stores hbStart/hbInterval as properties on the `ask`
function. Move them into the function closure (let bindings inside `ask`) to match
the pattern in `imessage.js`. Pure refactor; no behaviour change.

### BUG-5 (LOW) — impossible cron silent loop
`workspace/scheduler/scheduler.add.mjs` accepts any cron string. Reject impossible
expressions at add-time (use `nextCronDate(cron)` and reject if it returns null).
Also: in `scheduler.mjs`, if `nextCronDate` returns null for an already-stored job
during tick, log a warning AND disable the job so it stops the silent loop.

Allowed files to modify:
- `/Users/owner/secondme/workspace/scheduler/cron.mjs`
- `/Users/owner/secondme/workspace/scheduler/scheduler.mjs`
- `/Users/owner/secondme/workspace/scheduler/scheduler.add.mjs` (if it exists; else
  `workspace/tools/impl/scheduler.add.mjs`)
- `/Users/owner/secondme/workspace/memory/memory.mjs` (only if schema change needed)
- `/Users/owner/secondme/workspace/memory/migrate.mjs`
- `/Users/owner/secondme/index.js`
- `/Users/owner/secondme/workspace/tests/smoke.mjs` (add the regression tests below)

DO NOT touch:
- `/Users/owner/Helm/` — off-limits.
- `/Users/owner/secondme/workspace/tools/registry.json` — Phase 3 agent owns this.
- `/Users/owner/secondme/workspace/tools/impl/` (except scheduler.add.mjs).

Add smoke tests for each fix:
1. cron `0 2 * * *` next-fire is at UTC 02:00 (= 09:00 GMT+7), not local 02:00.
2. running migrate.mjs twice doesn't grow facts table.
3. tools.mjs call imessage.send (confirm:true) without --force exits non-zero.
4. (no smoke needed for the index.js refactor; node --check is enough.)
5. scheduler.add with impossible cron `0 0 30 2 *` returns non-zero.

After fixes run BOTH `workspace/tests/smoke.mjs` AND `workspace/tests/smoke-phase2.mjs`
and confirm green.

After daemon-affecting fixes (cron.mjs, scheduler.mjs), unload and reload
`com.helm.scheduler` via launchctl so the running daemon picks up the changes.

When done, write `/Users/owner/secondme/workspace/upgrades/BUGS_REPORT.md` summarising
what landed, what was deferred, and any new findings. Be blunt. No emojis, no flattery.
