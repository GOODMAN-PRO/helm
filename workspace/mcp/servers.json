{
  "mcpServers": {
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "__HELM_ROOT__"],
      "healthCheck": false,
      "enabled": true
    },
    "fetch": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-fetch"],
      "healthCheck": "initialize",
      "comment": "Disabled: that npm package does not exist (the official fetch server is Python-only); Claude Code's built-in WebFetch already covers this.",
      "enabled": false
    },
    "playwright": {
      "command": "npx",
      "args": ["-y", "@playwright/mcp@latest", "--headless"]
    },
    "github": {
      "command": "node",
      "args": ["__HELM_ROOT__/workspace/mcp/wrap-github.mjs"],
      "healthCheck": "initialize",
      "enabled": true
    },
    "google-workspace": {
      "command": "node",
      "args": ["__HELM_ROOT__/workspace/mcp/wrap-google-workspace.mjs"],
      "healthCheck": "initialize",
      "comment": "Disabled: upstream npm package @modelcontextprotocol/server-google-workspace no longer exists (404). Gmail is handled by Helm's own gmail.* tools (workspace/tools/impl/gmail.mjs).",
      "enabled": false
    },
    "brave-search": {
      "command": "node",
      "args": ["__HELM_ROOT__/workspace/mcp/wrap-brave-search.mjs"],
      "healthCheck": "initialize",
      "enabled": true
    },
    "video": {
      "command": "node",
      "args": ["__HELM_ROOT__/workspace/mcp/wrap-video.mjs"],
      "healthCheck": "initialize",
      "enabled": true
    }
  }
}
